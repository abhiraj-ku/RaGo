version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: standard
  enable:
    - errcheck
    - ineffassign
    - misspell
    - staticcheck

formatters:
  enable:
    - gofmt
    - goimports
